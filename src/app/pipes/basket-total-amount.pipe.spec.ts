import { BasketTotalAmountPipe } from './basket-total-amount.pipe';

describe('BasketTotalAmountPipe', () => {
  it('create an instance', () => {
    const pipe = new BasketTotalAmountPipe();
    expect(pipe).toBeTruthy();
  });
});
