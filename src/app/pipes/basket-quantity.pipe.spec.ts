import { BasketQuantityPipe } from './basket-quantity.pipe';

describe('BasketQuantityPipe', () => {
  it('create an instance', () => {
    const pipe = new BasketQuantityPipe();
    expect(pipe).toBeTruthy();
  });
});
